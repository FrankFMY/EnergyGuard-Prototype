# Демо для КАСТОР: Практичный План на 3 Недели (Фокус на Продажу)

**Обновлено:** 15 декабря 2025  
**Философия:** Продаем **картинку будущего**, а не продакшн-систему  
**Приоритет:** UI/UX > Архитектура для демо

---

## 🎯 Ключевая Стратегия

> **"Продаем вау-эффект, а не код"**

Демо должно показывать **ценность для бизнеса**, а не техническое совершенство. Клиент покупает **решение проблемы**, а не архитектуру.

---

## 🔥 Топ-3 Kill-Features (Главное для Продажи)

### 1. **Waterfall Chart "Деньги"** ⭐⭐⭐

**Почему это продает:**

- Директор видит **конкретную цифру потерь в рублях**
- Понятная визуализация: "Могли заработать" → "Потеряли на простое" → "Потеряли на плохом газе"
- Это главный виджет для CEO/финансового блока

**Реализация:**

- Используем ECharts Waterfall (есть встроенный тип)
- Данные: `{ ideal: 1000000, downtime: -200000, derating: -150000, actual: 650000 }`
- **Срок:** 1-2 дня

### 2. **Интерактивный Ползунок "Качество Газa"** ⭐⭐⭐

**Почему это продает:**

- Превращает презентацию в **интерактивную игру**
- Директор **сам** двигает ползунок → видит, как падают деньги
- Показывает: у вас есть **математическая модель**, а не просто графики

**Реализация:**

```svelte
<input type="range" min="0.7" max="1.0" step="0.01" bind:value={gasQuality} />
<!-- При изменении → реактивный пересчет всех графиков -->
```

- **Срок:** 1 день (добавить ползунок + реактивность)

### 3. **Синхронизированные Графики Корреляции** ⭐⭐

**Почему это продает:**

- Инженеры видят **причинно-следственную связь** в реальном времени
- "Вот тут скакнул газ → и ровно в эту секунду поползла температура"
- Делается в ECharts одной строчкой (`echarts.connect()`)

**Реализация:**

- 3 графика: Gas Quality, Temperature, Power
- `echarts.connect([chart1, chart2, chart3])`
- **Срок:** 1 день

---

## ❌ Что ВЫКИДЫВАЕМ (Чтобы Успеть)

### 1. Реальная Интеграция с 1С ❌

**Почему:**

- Убьет всё время (3 недели превратятся в 3 месяца)
- Для демо не нужно

**Вместо:**

- Мокаем данные: табличка `inventory_mock` в БД
- Говорим клиенту: "Вот так это будет выглядеть после интеграции"
- После подписания контракта — делаем реальную интеграцию

**Экономия:** 3-5 дней

### 2. BullMQ / Redis Queue ❌

**Почему:**

- Overengineering для демо
- Bun + SvelteKit справятся с 1000 сообщений/сек на демо-стенде
- Оптимизируем после подписания контракта

**Вместо:**

- Оставляем MQTT handler в `hooks.server.ts` (как есть)
- Работает отлично для демо

**Экономия:** 2-3 дня

### 3. Telegram-бот на Telegraf ❌

**Почему:**

- Не нужна библиотека для простой отправки сообщений
- Нам не нужно слушать сообщения, только отправлять

**Вместо:**

- Простой HTTP POST запрос:

```typescript
async function sendTelegramAlert(message: string) {
	await fetch(`https://api.telegram.org/bot${TOKEN}/sendMessage`, {
		method: 'POST',
		headers: { 'Content-Type': 'application/json' },
		body: JSON.stringify({ chat_id: CHAT_ID, text: message })
	});
}
```

**Экономия:** 0.5 дня (но проще код)

---

## 🛠 Обновленный План (3 Недели)

### **Неделя 1: Данные + Базовая Модель**

#### День 1-2: Расширение Схемы БД ⚡

- [x] Добавить `gas_quality_index` в `telemetry` таблицу
- [x] Создать таблицу `sites` (7 площадок) - упрощенная версия
- [x] Добавить `site_id` в `engines`
- [x] Миграция БД

**Фокус:** Минимум таблиц, максимум функциональности.

#### День 3-4: Модель "Газ → Температура → Мощность" ⚡

```typescript
// src/lib/server/services/gas-quality.service.ts
// Простая инженерная модель (линейная/полиномиальная)
// Не ML, просто формула на основе типовых ГПУ
```

**Фокус:** Работающая модель, а не идеальная.

#### День 5-7: Симулятор "Грязный Газ" ⚡

- Доработать `mock-device.ts`
- Сценарий: Утро (норма) → День (грязный газ) → Проблема (перегрев) → Решение (дерейтинг)

**Фокус:** Реалистичный сценарий, который показывает проблему.

---

### **Неделя 2: Визуализация (Главное!)**

#### День 8-9: Waterfall Chart ⭐⭐⭐

- [ ] Компонент `WaterfallChart.svelte`
- [ ] Интеграция в главный дашборд
- [ ] Данные: Идеал → Простои → Дерейтинг → Факт

**Это главный виджет для CEO. Делаем идеально.**

#### День 10-12: Синхронизированные Графики ⭐⭐

- [ ] 3 графика: Gas Quality, Temperature, Power
- [ ] `echarts.connect()` для синхронизации курсора
- [ ] Tooltip показывает все 3 значения одновременно
- [ ] Плавная анимация при обновлении

#### День 13-14: Схема Двигателя (SVG) ⭐

- [ ] Найти готовую SVG схему V-образного двигателя
- [ ] Динамическая раскраска цилиндров (градиент от зеленого к красному)
- [ ] Реактивность: температура → цвет

**Фишка:** Находим готовый SVG, не рисуем сами.

---

### **Неделя 3: Полировка + Интерактивность**

#### День 15-16: Интерактивный Ползунок ⭐⭐⭐

- [ ] Ползунок "Качество газа" на детальной странице двигателя
- [ ] При изменении → пересчет всех метрик (реактивность Svelte)
- [ ] Графики обновляются в реальном времени
- [ ] Показываем инсайт: "При таком газе потеряете X руб/час"

**Это kill-feature для продажи. Делаем идеально.**

#### День 17-18: Telegram-бот (Упрощенный) ⚡

- [ ] HTTP POST к Telegram API (без библиотек)
- [ ] Отправка при критических алертах
- [ ] Форматирование сообщения (красивое)

**Простой, но работает.**

#### День 19-20: Темная Тема + Полировка ⚡

- [ ] Dark Industrial дизайн (Tailwind `gray-900`, акцентные цвета)
- [ ] Моноширинные шрифты для цифр (JetBrains Mono)
- [ ] Плавные CSS transitions на всех элементах
- [ ] Эффект "живых данных" (даже если данные приходят рывками, UI плавный)

#### День 21: Тестирование + Деплой

- [ ] Прогон всего сценария
- [ ] Проверка на мобильном
- [ ] Деплой на VDS
- [ ] Подготовка презентации

---

## 💡 Технические Детали (Упрощенные)

### 1. Waterfall Chart (ECharts)

```typescript
// Простая конфигурация
const option = {
	series: [
		{
			type: 'bar', // Waterfall через обычный bar с отрицательными значениями
			data: [
				{ value: 1000000, name: 'Идеал', itemStyle: { color: '#10b981' } },
				{ value: -200000, name: 'Простои', itemStyle: { color: '#ef4444' } },
				{ value: -150000, name: 'Дерейтинг', itemStyle: { color: '#f59e0b' } },
				{ value: 650000, name: 'Факт', itemStyle: { color: '#3b82f6' } }
			],
			stack: 'money'
		}
	]
};
```

Или используем готовый ECharts Waterfall (если есть в версии).

### 2. Интерактивный Ползунок

```svelte
<!-- src/routes/engine/[id]/+page.svelte -->
<script lang="ts">
	let gasQualityDemo = $state(1.0);

	// Реактивные вычисления
	const theoreticalPower = $derived(calculatePowerFromGasQuality(gasQualityDemo, nominalPower));
	const theoreticalTemp = $derived(calculateTempFromGasQuality(gasQualityDemo, baseTemp));
	const lostRevenue = $derived((nominalPower - theoreticalPower) * tariffPerHour);
</script>

<label>Качество газа (демо): {gasQualityDemo.toFixed(2)}</label>
<input type="range" min="0.7" max="1.0" step="0.01" bind:value={gasQualityDemo} class="w-full" />

<!-- Графики автоматически обновляются через реактивность -->
<CorrelationChart
	gasQuality={gasQualityDemo}
	temperature={theoreticalTemp}
	power={theoreticalPower}
/>
```

### 3. Telegram-бот (10 строк кода)

```typescript
// src/lib/server/services/telegram.service.ts
const TELEGRAM_BOT_TOKEN = env.TELEGRAM_BOT_TOKEN;
const TELEGRAM_CHAT_ID = env.TELEGRAM_CHAT_ID;

export async function sendTelegramAlert(
	engineId: string,
	message: string,
	severity: 'warning' | 'critical'
) {
	const emoji = severity === 'critical' ? '🚨' : '⚠️';
	const text = `${emoji} ${message}\nДвигатель: ${engineId}`;

	await fetch(`https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage`, {
		method: 'POST',
		headers: { 'Content-Type': 'application/json' },
		body: JSON.stringify({
			chat_id: TELEGRAM_CHAT_ID,
			text,
			parse_mode: 'Markdown'
		})
	});
}
```

**Никаких библиотек. Работает отлично.**

### 4. SVG Двигателя (Готовый)

**Где найти:**

- Google: "V12 engine SVG diagram" или "V16 gas engine SVG"
- SVG Repo, Flaticon (с атрибуцией)
- Или попросить дизайнера нарисовать простую схему (2 часа работы)

**В Svelte:**

```svelte
<!-- src/lib/components/engine/EngineDiagram.svelte -->
<script lang="ts">
	// Пропсы: температуры по цилиндрам
	interface Props {
		cylinderTemps: number[]; // [450, 480, 520, 460, ...]
	}

	function tempToColor(temp: number): string {
		if (temp < 500) return '#10b981'; // green
		if (temp < 530) return '#f59e0b'; // orange
		return '#ef4444'; // red
	}
</script>

<svg viewBox="0 0 800 600">
	<!-- Вставить готовый SVG код -->
	<path
		id="cylinder-1"
		d="M100,100..."
		fill={tempToColor(cylinderTemps[0])}
		class="transition-colors duration-500"
	/>
	<!-- ... остальные цилиндры -->
</svg>
```

### 5. Эффект "Живых Данных"

```css
/* В глобальном CSS или компонентах */
.number-display {
	transition: all 0.5s ease;
}

.chart-container {
	transition: opacity 0.3s ease;
}

/* Даже если данные приходят рывками, UI плавный */
```

---

## 🎨 UI/UX Спецификация (Обновлено)

### Цветовая Схема

```css
/* Tailwind классы */
--bg-primary: bg-gray-900; /* #111827 */
--bg-secondary: bg-gray-800; /* #1f2937 */
--accent-green: bg-emerald-500; /* #10b981 */
--accent-yellow: bg-amber-500; /* #f59e0b */
--accent-red: bg-red-500; /* #ef4444 */
--accent-blue: bg-blue-500; /* #3b82f6 */

/* Моноширинные шрифты для цифр */
font-family: 'JetBrains Mono', 'Roboto Mono', monospace;
```

### Анимации

```css
/* Пульсация при критическом статусе */
@keyframes pulse-red {
	0%,
	100% {
		box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
	}
	50% {
		box-shadow: 0 0 0 10px rgba(239, 68, 68, 0);
	}
}

.critical-pulse {
	animation: pulse-red 2s infinite;
}
```

---

## 📊 Приоритизация (Что Делать Первым)

### Must Have (Критично для продажи):

1. ✅ **Waterfall Chart** - главный виджет для CEO
2. ✅ **Интерактивный ползунок** - kill-feature
3. ✅ **Синхронизированные графики** - для инженеров
4. ✅ **Расчет потерь в рублях** - связь техники и денег

### Should Have (Важно, но можно упростить):

5. ⚠️ Схема двигателя (SVG) - nice to have
6. ⚠️ Telegram-бот - простая версия (HTTP POST)
7. ⚠️ Темная тема - базовый вариант (Tailwind)

### Nice to Have (Если останется время):

8. 📌 Мобильная адаптация (базовая)
9. 📌 Многоплощадочная структура (можно на демо показать заглушку)

---

## 🚀 Следующие Шаги

**Начинаем с:**

1. Расширение БД (gas_quality_index, sites) - 2 дня
2. Модель газа → температура → мощность - 2 дня
3. Waterfall Chart - 2 дня (главное!)

**Затем:** 4. Симулятор "грязный газ" - 1 день 5. Синхронизированные графики - 1 день 6. Интерактивный ползунок - 1 день

**В итоге:** За 2 недели получаем 90% продающего функционала.

---

## 💬 Итоговый Вердикт

**План отличный, но нужно:**

1. ✅ Фокусироваться на **UI/UX** (Waterfall, ползунок, графики)
2. ✅ Выкинуть сложное (1С, очереди, библиотеки для Telegram)
3. ✅ Делать **быстро и продающе**, а не "правильно архитектурно"

**Главное:** Продаем **картинку будущего**, а не продакшн-систему. Демо должно вызывать **"Вау! Я хочу это!"**, а не "Интересная архитектура".

---

**Готов начинать? Предлагаю стартовать с расширения БД + модели газа. Это база для всего остального.**
