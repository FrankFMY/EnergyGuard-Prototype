# Демо-стенд для КАСТОР: Roadmap на 3 недели

**Дата:** 15 декабря 2025  
**Цель:** Интерактивный симулятор проблем для "вау-эффекта"  
**Фокус:** Связь Техники → Деньги (Газ → Температура → Мощность → Потери)

---

## ✅ Что УЖЕ Есть (Базис)

### Архитектура ✅
- ✅ SvelteKit + Bun + TimescaleDB + Redis + EMQX
- ✅ MQTT интеграция (прием данных)
- ✅ SSE для real-time (с fallback на polling)
- ✅ Базовая структура дашборда

### Функционал ✅
- ✅ Расчет потерь (`currentLoss` в `engine.service.ts`)
- ✅ OEE виджеты (базовый расчет)
- ✅ Downtime tracking (структура в БД)
- ✅ Efficiency расчеты
- ✅ Profit rate расчеты
- ✅ Gas consumption метрики
- ✅ Mock device симулятор

**Вывод:** У вас **70% уже готово**. Нужно доработать специфику под КАСТОР.

---

## 🎯 Что НУЖНО Добавить (Для Демо КАСТОР)

### Критично (Неделя 1-2)

#### 1. **Gas Quality Index (Качество Газa)** ❌ → ✅
**Проблема:** В текущей схеме нет поля `gas_quality_index`  
**Решение:**
```typescript
// src/lib/server/db/schema.ts - добавить в telemetry:
gas_quality_index: doublePrecision('gas_quality_index') // 0.7-1.0

// src/lib/types/telemetry.ts:
gas_quality_index: z.number().min(0.5).max(1.0) // 1.0 = чистый, 0.7 = грязный
```

#### 2. **Многоплощадочная Структура (7 площадок)** ❌ → ✅
**Проблема:** Сейчас только `engines`, нет группировки по площадкам  
**Решение:**
```typescript
// Новая таблица sites:
export const sites = pgTable('sites', {
  id: text('id').primaryKey(),
  name: text('name').notNull(), // "Площадка 'Восточная'"
  total_capacity_mw: doublePrecision('total_capacity_mw'),
  region: text('region')
});

// Связь engine -> site:
engine.site_id: text('site_id').references(() => sites.id)
```

#### 3. **Модель "Газ → Температура → Мощность"** ⚠️ → ✅
**Проблема:** Есть расчеты, но нет **причинно-следственной связи**  
**Решение:** Доработать симулятор и расчеты

```typescript
// src/lib/server/services/gas-quality.service.ts
export function calculateDeratingFromGasQuality(
  gasQuality: number, // 0.7-1.0
  nominalPower: number
): number {
  // При ухудшении газа (0.7) - мощность падает на 20-30%
  const deratingFactor = 0.7 + (gasQuality - 0.7) * 1.0; // Линейная модель
  return nominalPower * deratingFactor;
}

export function calculateTempFromGasQuality(
  gasQuality: number,
  baseTemp: number = 450
): number {
  // Грязный газ (0.7) → температура растет на 50-70°C
  const tempIncrease = (1.0 - gasQuality) * 100; // Макс +100°C
  return baseTemp + tempIncrease;
}
```

#### 4. **Waterfall Chart Потерь** ❌ → ✅
**Проблема:** Есть расчет `currentLoss`, но нет визуализации "Идеал → Простои → Дерейтинг → Факт"  
**Решение:** Новый компонент

```svelte
<!-- src/lib/components/dashboard/WaterfallChart.svelte -->
<script lang="ts">
  import * as echarts from 'echarts';
  
  // Данные:
  // { ideal: 100000, downtime: -20000, derating: -30000, actual: 50000 }
</script>
```

#### 5. **Синхронизированные Графики Корреляции** ⚠️ → ✅
**Проблема:** Есть отдельные графики, нет синхронизации курсора  
**Решение:** Использовать ECharts `connect()` API

#### 6. **Сценарий Симулятора "Грязный Газ"** ⚠️ → ✅
**Проблема:** Текущий симулятор делает "вибрация → перегрев", а нужно "газ → температура → мощность"  
**Решение:** Доработать `mock-device.ts`

---

### Важно (Неделя 2-3)

#### 7. **Интерактивный Ползунок "Качество Газa"** ❌ → ✅
**Фишка для демо:** Показывать "что если" в реальном времени

#### 8. **Визуализация Цилиндров Двигателя** ❌ → ✅
**Фишка:** SVG схема V-образного двигателя с градиентом по температуре

#### 9. **Расчетная vs Фактическая Выработка** ⚠️ → ✅
**Проблема:** Есть базовая "efficiency", но нет явного сравнения "что должно быть vs что есть"  
**Решение:**
```typescript
// Расчетная выработка на основе газа:
calculatedPower = f(gasQuality, gasConsumption, nominalEfficiency)
actualPower = текущая мощность
loss = calculatedPower - actualPower
```

#### 10. **Telegram-бот для Алертов** ❌ → ✅
**Критично для продаж!** Клиент ожидает уведомления

---

## 📅 План Разработки (3 Недели)

### **Неделя 1: Данные и Модель**

#### День 1-2: Расширение Схемы БД
- [ ] Добавить `gas_quality_index` в `telemetry`
- [ ] Создать таблицу `sites` (7 площадок)
- [ ] Добавить `site_id` в `engines`
- [ ] Миграция БД

#### День 3-4: Модель "Газ → Температура → Мощность"
```typescript
// Создать новый сервис:
src/lib/server/services/gas-quality.service.ts

// Функции:
- calculateDeratingFromGasQuality()
- calculateTempFromGasQuality()
- calculateTheoreticalPower() // Расчетная мощность
- calculateActualVsTheoreticalLoss() // Потери
```

#### День 5-7: Доработка Симулятора
```typescript
// scripts/mock-device.ts - добавить сценарий:

// Сценарий для площадки #1 (GPU-1):
// 08:00 - Нормальный режим (gas_quality = 1.0)
// 12:00 - Приходит "тяжелый" газ (gas_quality падает до 0.75)
// 12:30 - Температура растет до 520°C
// 13:00 - Автоматика снижает мощность до 80%
// 14:00 - Газ улучшается, восстановление

// Генерировать данные с учетом связи:
gas_quality → temp_increase → power_derating → money_lost
```

---

### **Неделя 2: Визуализация**

#### День 8-10: Главный Дашборд (Executive View)

**Экран 1: "Здоровье Бизнеса"**
- [ ] Waterfall Chart: Идеал → Простои → Дерейтинг → Факт
- [ ] Большая цифра: Выручка за сегодня (руб)
- [ ] Красным: Упущенная выгода
- [ ] Карточки 7 площадок (Grid) с пульсацией при перегреве

```svelte
<!-- src/routes/+page.svelte - доработать -->
<!-- Добавить новый виджет -->
<FinancialHealthWidget 
  {revenue} 
  {lostRevenue} 
  {downtimeLoss} 
  {deratingLoss} 
/>
```

#### День 11-14: Детальная Аналитика (Engineer View)

**Экран 2: "Корреляция Газ → Температура → Мощность"**
- [ ] Три синхронизированных графика (ECharts `connect()`)
  - Верхний: Gas Quality Index (0.7-1.0)
  - Средний: Температура выхлопа
  - Нижний: Активная мощность (кВт)
- [ ] Вертикальная линия при наведении (синхронизация)
- [ ] Tooltip показывает все 3 значения одновременно

```svelte
<!-- src/lib/components/charts/CorrelationChart.svelte -->
<script lang="ts">
  // Использовать ECharts group + connect
  const chartInstances = [gasChart, tempChart, powerChart];
  echarts.connect(chartInstances);
</script>
```

**Экран 3: Схема Двигателя**
- [ ] SVG схема V-образного двигателя (6-8 цилиндров)
- [ ] Градиентная раскраска по температуре (зеленый → красный)
- [ ] Подсветка проблемного цилиндра

```svelte
<!-- src/lib/components/engine/EngineDiagram.svelte -->
<!-- SVG с динамической раскраской -->
```

---

### **Неделя 3: Полировка и Интерактивность**

#### День 15-17: Интерактивный Симулятор

**Фишка "Что если":**
- [ ] Ползунок "Качество Газa" (0.7-1.0) на детальной странице
- [ ] При движении ползунка:
  - Графики перестраиваются в реальном времени
  - Расчетная мощность пересчитывается
  - Потери в рублях обновляются
  - Показывается инсайт: "При таком газе потеряете X руб/час"

```svelte
<!-- src/routes/engine/[id]/+page.svelte -->
<label>Качество газа (для демо):</label>
<input 
  type="range" 
  min="0.7" 
  max="1.0" 
  step="0.01" 
  bind:value={gasQualityDemo}
/>
<!-- При изменении → пересчет через реактивность -->
```

#### День 18-19: Telegram-бот

```typescript
// src/lib/server/services/telegram.service.ts
import { Telegraf } from 'telegraf';

export async function sendAlertToTelegram(
  engineId: string,
  message: string,
  severity: 'warning' | 'critical'
) {
  // Отправка в Telegram
  // Интеграция с alert.service.ts
}
```

#### День 20-21: Финальная Полировка

- [ ] Темная тема (Dark Mode Industrial)
- [ ] Моноширинные шрифты для цифр (JetBrains Mono)
- [ ] Плавные анимации (NumberTicker уже есть ✅)
- [ ] Мобильная адаптивность
- [ ] Тестирование на реальных данных

---

## 🗂️ Структура Новых Файлов

```
src/
├── lib/
│   ├── server/
│   │   ├── services/
│   │   │   ├── gas-quality.service.ts  ⭐ НОВЫЙ
│   │   │   ├── telegram.service.ts     ⭐ НОВЫЙ
│   │   │   └── financial-loss.service.ts ⭐ НОВЫЙ (детальный расчет)
│   │   └── db/
│   │       └── schema.ts                📝 ДОРАБОТАТЬ (sites, gas_quality_index)
│   ├── components/
│   │   ├── dashboard/
│   │   │   ├── WaterfallChart.svelte    ⭐ НОВЫЙ
│   │   │   ├── FinancialHealthWidget.svelte ⭐ НОВЫЙ
│   │   │   └── SiteCardsGrid.svelte     ⭐ НОВЫЙ
│   │   ├── charts/
│   │   │   └── CorrelationChart.svelte  ⭐ НОВЫЙ (3 синхронизированных)
│   │   └── engine/
│   │       └── EngineDiagram.svelte     ⭐ НОВЫЙ (SVG)
│   └── types/
│       └── gas-quality.ts               ⭐ НОВЫЙ
├── routes/
│   ├── engine/
│   │   └── [id]/
│   │       └── +page.svelte             📝 ДОРАБОТАТЬ (добавить ползунок)
│   └── sites/
│       └── [id]/
│           └── +page.svelte             ⭐ НОВЫЙ (детальная страница площадки)
└── scripts/
    └── mock-device.ts                   📝 ДОРАБОТАТЬ (сценарий "грязный газ")
```

---

## 💡 Технические Детали

### 1. Gas Quality Model (Физика)

```typescript
// Модель основана на типовых ГПУ:
// - Чистый ПНГ (CH4 >95%): quality = 1.0 → нормальная мощность
// - "Тяжелый" ПНГ (C3H8/C4H10 примеси): quality = 0.75 → мощность -20%, температура +50°C
// - Очень грязный: quality = 0.7 → мощность -30%, температура +70°C

export function gasQualityToPowerDerating(quality: number): number {
  // Полиномиальная модель (на основе реальных данных ГПУ)
  const deratingPercent = (1.0 - quality) * 0.4; // Макс -40% при quality=0.6
  return 1.0 - deratingPercent;
}

export function gasQualityToTempIncrease(quality: number): number {
  // Линейная модель
  const tempIncrease = (1.0 - quality) * 100; // +100°C при quality=0.0 (нереально, но для модели)
  return Math.min(tempIncrease, 80); // Ограничение +80°C
}
```

### 2. Waterfall Chart (ECharts)

```typescript
// Пример конфигурации:
const option = {
  series: [{
    type: 'waterfall',
    data: [
      { value: 100000, name: 'Идеал (100%)' },
      { value: -20000, name: 'Простои' },
      { value: -30000, name: 'Дерейтинг (грязный газ)' },
      { value: 50000, name: 'Факт' }
    ]
  }]
};
```

### 3. Синхронизация Графиков (ECharts)

```typescript
import * as echarts from 'echarts';

// Создать 3 инстанса
const gasChart = echarts.init(gasChartDom);
const tempChart = echarts.init(tempChartDom);
const powerChart = echarts.init(powerChartDom);

// Связать
echarts.connect([gasChart, tempChart, powerChart]);

// При наведении - все 3 графика показывают tooltip одновременно
```

### 4. Telegram-бот (Минималистичный)

```typescript
import { Telegraf } from 'telegraf';

const bot = new Telegraf(env.TELEGRAM_BOT_TOKEN);

// При создании критического алерта:
await bot.telegram.sendMessage(
  chatId,
  `🚨 КРИТИЧНО: ${engine.name}\n` +
  `Температура: ${temp}°C (порог: ${threshold}°C)\n` +
  `Потеря мощности: ${powerLoss} кВт\n` +
  `Упущенная выгода: ${lostRevenue} руб/час`
);
```

---

## 🎨 UI/UX Спецификация

### Цветовая Схема (Dark Industrial)

```css
/* Темный фон */
--bg-primary: #1a1a1a;
--bg-secondary: #252525;

/* Акценты */
--accent-green: #10b981;   /* Работает нормально */
--accent-yellow: #f59e0b;  /* Предупреждение */
--accent-red: #ef4444;     /* Критично */
--accent-blue: #3b82f6;    /* Информация */

/* Текст */
--text-primary: #f9fafb;
--text-secondary: #9ca3af;
--text-mono: 'JetBrains Mono', monospace; /* Для цифр */
```

### Анимации

- **Пульсация карточек:** При критическом статусе (`animation: pulse 2s infinite`)
- **NumberTicker:** Плавная анимация изменения цифр (уже есть ✅)
- **Градиент цилиндров:** Плавный переход цвета (CSS `transition`)

---

## 📊 Метрики Успеха Демо

**Критерии "вау-эффекта":**

1. ✅ **Визуальная связь:** Директор видит "грязный газ → потеря 120 000 руб за смену"
2. ✅ **Интерактивность:** Ползунок "газ" → графики меняются в реальном времени
3. ✅ **Понятность:** Инженер видит причину (графики корреляции)
4. ✅ **Мобильность:** Работает на телефоне директора
5. ✅ **Скорость:** Мгновенный отклик (SSE + кэширование)

**Вопросы после демо (ожидаемые):**
- "Как быстро можно внедрить?"
- "Сколько стоит?"
- "Когда начнем?"

---

## ⚠️ Риски и Митигация

### Риск 1: Нет реальных данных о качестве газа
**Митигация:** Используем инженерную модель (регрессия) на основе типовых ГПУ. Для демо - достаточно.

### Риск 2: Клиент не увидит "вау-эффект"
**Митигация:** 
- Готовить презентацию со скриншотами заранее
- Прогнать демо на коллегах перед показом
- Подготовить "ключевые моменты" для комментариев

### Риск 3: Не хватит времени на все фишки
**Приоритизация:**
1. **Must have:** Gas Quality + Waterfall Chart + Синхронизированные графики
2. **Should have:** Интерактивный ползунок + Telegram
3. **Nice to have:** Схема двигателя + мобильная адаптация

---

## 🚀 Следующие Шаги (После Демо)

Если демо успешно:

1. **Уточнить требования:**
   - Какие протоколы у их SCADA (Modbus/OPC UA)?
   - Есть ли измерение качества газа в реальном времени?
   - Интеграция с 1С (склад ЗИП)?

2. **Пилотный проект:**
   - Подключить 1-2 площадки
   - Начать сбор реальных данных
   - Обучение моделей на исторических данных

3. **Масштабирование:**
   - Все 7 площадок
   - ML-модели для прогнозирования
   - Интеграция с майнингом (второй этап)

---

## 💬 Вопросы для Клиента (Перед Демо)

**Технические:**
1. Есть ли SCADA/АСУ ТП на площадках? Какие протоколы?
2. Где хранится история данных (архиватор/Excel/журналы)?
3. Есть ли измерение расхода и состава газа в реальном времени?
4. Где фиксируют ТО/ремонты сейчас (1С/Excel)?

**Бизнес:**
1. Какая главная единица эффективности (руб/сутки, МВт·ч/сутки, КПД)?
2. Как сейчас принимают решение "снизить температуру" (порог, где записано)?
3. Что считается простоем (останов, дерейтинг, работа ниже номинала)?
4. Есть ли норматив "ожидаемой выработки" при данном качестве ПНГ?

**Организация:**
1. Кто владелец продукта (главный энергетик/финансы)?
2. Кто будет ежедневно пользоваться системой?
3. Нужны ли мобильные уведомления (Telegram/SMS)?

---

## ✅ Чеклист Готовности

**За 3 дня до демо:**
- [ ] Все компоненты собраны и работают
- [ ] Симулятор генерит реалистичный сценарий
- [ ] Водопад потерь корректен
- [ ] Синхронизация графиков работает
- [ ] Telegram-бот отправляет тестовые сообщения
- [ ] Демо на VDS развернуто и доступно
- [ ] Презентация подготовлена
- [ ] Скриншоты ключевых экранов готовы

**За 1 день до демо:**
- [ ] Прогон на коллегах
- [ ] Проверка на мобильном устройстве
- [ ] Подготовка ответов на типовые вопросы
- [ ] Резервный план (если что-то сломается)

---

**Удачи с демо! 🚀**

Этот план реалистичен и основан на том, что у вас уже есть. Основная работа - доработать специфику под КАСТОР, а не строить с нуля.
